# ✅ 影视墙过滤规则优化 — 已完成

---

## 🔬 评分可信度：IMDb 贝叶斯加权（2026-09-03）

### 文件路径
- `E:/Desktop/kims_video/1.4- Intelligence/workers-1.4.js`
- `E:/Desktop/kims_video/1.4- Intelligence/src/components/IntelConfigPanel.jsx`

### 背景
真实数据验证（GitHub Actions run 33716938299，TMDB zh-CN）发现：电影非CN池极小（仅6部，中位24票）、其中一半票数<20；个别条目如「狂热先生」仅1票却评9.0，裸投票分虚高。引入贝叶斯加权让"评分可信度随票数增加而提高"。

### 公式（IMDb 贝叶斯加权，0-10 分制）
```
R加权 = (v/(v+m))×R + (m/(v+m))×C
  R = TMDB vote_average，v = vote_count，m = 最小票数阈值，C = 中性基准分
```
票数≥m → R加权≈R（可信）；票数→0 → R加权→C（被拉沉，刷分无效）。

### 实测定标（m=20, C=6.0）
| 条目 | 评分 | 票数 | 原→加权 |
|---|---|---|---|
| 狂热先生 | 9.0 | 1 | 90 → 61 |
| 早春晴朗 | 6.7 | 5 | 67 → 61 |
| 绿灯军团 | 8.271 | 242 | 83 → 81 |
| 侠探杰克 | 8.104 | 3201 | 81 → 81（不变）|
| 瑞克和莫蒂 | 8.682 | 11349 | 87 → 87（不变）|

### 修改点
1. **`DEFAULT_INTEL_CFG.credibility`（`:335`）** 新增共享配置组：
   ```js
   credibility: { minVotes: 20, meanRating: 6.0, cnMinVotes: 20, minVoteCountHard: false }
   ```
   备注：实测国产池中位数95反高于非CN（24），故 `cnMinVotes` 不额外放宽、与全局一致；`minVoteCountHard` 硬下限默认关（贝叶斯已够）。
2. **scoreOpts 注入 cred**（`movieSc/tvSc` 及3处 `scoreOpts` 构造）：`cred: cfg.credibility` 随 opts 传入 `intelComputeScore`。
3. **`intelComputeScore` S_qual（`:371` 附近）**：R加权替代裸 `vote_average`（电影+剧集共用）。
4. **`gateNowPlaying`（复合门槛）**：`rate` 改用 R加权（非CN）；国产池仍走宽松 `cnFloorRating` 门槛、不受硬下限影响。
5. **TV ongoing 可选硬下限（`:1820`）**：`minVoteCountHard=true` 时剔除票数<m 条目（国产用 cnMinVotes）。
6. **Admin 面板**：新增独立「评分可信度 · 贝叶斯加权」分组 + 公式框；gate/movie/TV 公式框补一行说明"评分=贝叶斯加权"，指标分类不混淆。

### 豁免
- upcoming / premiere / TVMAZE 来源天然无票 → 不参与加权（TVMAZE 条目无 vote_count）。

---

## 📋 修改内容

### 文件路径
`E:/Desktop/kims_video/1.4- Intelligence/scripts/fetch-intelligence-data.js`

### 新增功能

#### 1️⃣ 国产地区电影识别函数（约第 236-256 行）

```javascript
/**
 * Detect if movie is from China mainland / Hong Kong / Taiwan
 * Based on TMDB originCountry codes or title keywords
 */
function isChineseRegionMovie(item) {
  const origin = Array.isArray(item.originCountry) ? item.originCountry : [];
  
  // Direct country match: CN, HKG,TWN (含香港、台湾)
  if (origin.includes('CN') || origin.includes('HKG') || origin.includes('TWN')) {
    return true;
  }
  
  // Title keywords indicating Chinese region release
  const zhRegionKeywords = ['内地上映', '中国大陆', '中国香港', '中国台湾', 
                            'HK 上映 ', 'TVB', 'Hong Kong', 'Taiwan film'];
  if (zhRegionKeywords.some(kw => item.title && item.title.includes(kw))) {
    return true;
  }
  
  return false;
}
```

#### 2️⃣ 修改过滤逻辑（约第 63-75 行）

**原有规则：**
```javascript
// ❌ title AND summary 必须同时含中文
return check(item.title || item.name) &&
       check(item.summary || item.overview);
```

**新规则：**
```javascript
// ✅ 国产电影：只要 title 有中文即可
if (isChineseRegionMovie(item)) {
  return hasChinese(item.title || item.name);  // NO summary check required
}

// 其他地区：保持原有严格规则
return hasChinese(item.title || item.name) &&
       hasChinese(item.summary || item.overview);
```

---

## 🎯 效果预期

### 受益电影类型

| 类型 | 例子 | 说明 |
|------|------|------|
| **大陆院线片** | 《热辣滚烫》《第二十条》 | originCountry: CN 或内地上映关键词可识别 |
| **港产片** | PSV、TVB 电影 | HKG/TWN 代码或直接通过"香港”关键词识别 |
| **台产片** | 台湾本土制作 | TWN 代码匹配 |

### 收录对比（修改前 vs 后）

| 状况 | 过滤要求 | 《欢迎来到龙餐馆》这类影片能否入库 |
|------|---------|-----------------------------------|
| ❌ 原规则 | title+summary 同时含中文 | ✗ 未收录 |
| ✅ 新规则 | 国产片只要中文名即可 | ✓ 立即收录 |

---

## 📊 测试建议

### 方法 1: 手动触发同步脚本

```bash
cd "E:/Desktop/kims_video/1.4- Intelligence/"
node scripts/fetch-intelligence-data.js
```

**查看输出：**
```bash
# 如果新电影被收录，应该看到：
OK movies.json — NEW DATA
OK wall.json — +1 new, ... total
```

### 方法 2: 检查 movies.json

编辑后直接运行脚本，查看 `public/api/movies.json` 是否收录《欢迎来到龙餐馆》

---

## ⚠️ 注意事项

1. **不会影响现有国际电影**——非国产影片仍按原规则处理
2. **过滤条件更宽松**——国产电影即使没有中文简介也能入库
3. **下次同步自动生效**——无需重新部署 CF Worker

---

## 📅 后续步骤

- [ ] 运行同步脚本验证（见上面测试建议）  
- [ ] 检查 `E:/movies_live.json` 或 wall.json 是否收录成功  
- [ ] 如需更多地区关键词（如"内地上映""中国台北"），可继续扩充 `zhRegionKeywords` 数组

---

## ✅ 完成

**修改已应用，国产电影不再要求中文简介即可收录！**
